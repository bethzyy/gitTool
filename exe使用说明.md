# Git GUI 提交工具 - EXE 版本

## 📦 文件说明

**文件名**：`GitGUI提交工具.exe`
**位置**：`dist/GitGUI提交工具.exe`
**大小**：约 9.7 MB
**类型**：Windows 可执行文件（独立运行）

## 🚀 使用方法

### 方式一：直接运行（推荐）

1. 进入 `dist` 文件夹
2. 双击 `GitGUI提交工具.exe`
3. 应用程序启动，无命令行窗口

### 方式二：创建快捷方式

为了更方便使用，可以创建快捷方式：

1. 右键点击 `GitGUI提交工具.exe`
2. 选择"发送到" → "桌面快捷方式"
3. 以后可以直接从桌面双击运行

### 方式三：固定到任务栏

1. 将快捷方式创建到桌面
2. 右键点击桌面快捷方式
3. 选择"固定到任务栏"
4. 以后可以一键启动

## ✨ 功能特点

- ✅ **独立运行** - 无需安装 Python 环境
- ✅ **无窗口启动** - 只显示图形界面，无命令行窗口
- ✅ **单文件** - 所有依赖打包在一个 exe 中
- ✅ **清晰界面** - 清新蓝色主题，美观易用
- ✅ **安全扫描** - 默认开启，防止敏感信息泄露
- ✅ **完整日志** - 显示所有 Git 命令

## 📋 前置要求

虽然 exe 文件独立运行，但仍需要：

1. **Git** - [下载地址](https://git-scm.com/downloads)
2. **GitHub SSH 密钥** 或 **Personal Access Token**
3. **配置 Git 用户信息**：
   ```bash
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```

## 🔧 如何修改 GitHub 用户名

默认使用的用户名是 `bethzyy`，如需修改：

1. 使用文本编辑器打开源码 `git_gui_app.py`
2. 找到第 222 行：
   ```python
   repo_url = f"git@github.com:bethzyy/{repo_name}.git"
   ```
3. 将 `bethzyy` 改为你的 GitHub 用户名
4. 重新运行 `build_exe.bat` 打包

## 📦 重新打包

如果修改了代码，需要重新打包：

### 方式一：使用打包脚本（推荐）

双击运行：`build_exe.bat`

### 方式二：手动打包

```bash
pip install pyinstaller
python -m PyInstaller --onefile --windowed --name "GitGUI提交工具" git_gui_app.py
```

打包完成后，新的 exe 文件在 `dist` 文件夹中。

## 📁 文件结构

```
gitTool/
│
├── GitGUI提交工具.exe       ← 主程序（在 dist 文件夹中）
│
├── git_gui_app.py          ← 源代码
├── build_exe.bat           ← 打包脚本
│
├── build/                  ← 打包临时文件（可删除）
└── dist/                   ← 分发文件
    └── GitGUI提交工具.exe   ← 最终的 exe 文件
```

## ⚠️ 注意事项

- exe 文件约 9.7 MB，包含了所有依赖
- 首次运行可能被杀毒软件拦截，需要添加信任
- Windows Defender 可能提示"未知发布者"，这是正常的
- 如需分发，可以只提供 exe 文件，无需其他文件

## 🔍 杀毒软件信任

如果杀毒软件拦截：

1. Windows Defender：
   - 打开"病毒和威胁防护"
   - 点击"保护历史"
   - 找到被拦截的记录
   - 点击"操作" → "允许"

2. 其他杀毒软件：
   - 添加到白名单
   - 或暂时关闭实时保护

## 📝 版本信息

- **版本**：v1.1.0
- **打包日期**：2025-12-30
- **Python 版本**：3.11.9
- **PyInstaller 版本**：6.17.0

## 💡 常见问题

### Q: exe 文件打不开？

**A:**
1. 检查是否安装了 Git
2. 查看是否有错误提示
3. 尝试以管理员身份运行

### Q: 打包后文件很大？

**A:** 这是正常的，exe 文件包含了：
- Python 运行时
- Tkinter GUI 库
- 所有依赖库

### Q: 如何减小文件大小？

**A:** 可以尝试：
1. 使用 UPX 压缩（需要额外安装）
2. 移除未使用的模块（需要修改打包配置）
3. 但通常不建议，可能影响功能

### Q: 可以分发给其他人吗？

**A:** 可以！exe 文件独立运行，只需：
1. 对方安装了 Git
2. 配置了 GitHub 密钥
3. 直接双击运行即可

---

**享受一键启动的便捷体验！** 🎉
